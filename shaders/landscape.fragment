#ifdef GL_ES
precision highp float;
#endif

varying vec4 vColour;
varying vec2 vTextureCoords;

uniform sampler2D uSampler;

void main(void) {
  gl_FragColor = vColour * texture2D(uSampler, vec2(vTextureCoords.s, vTextureCoords.t));
}
