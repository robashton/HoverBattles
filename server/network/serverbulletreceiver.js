
var ServerBulletReceiver = function(app) {
  this.app = app;  
};


ServerBulletReceiver.prototype._fire = function(data) {
  
  // Get the entity referred to
  
  // Add the bullet to the scene (if possible)
  
  // Send out a confirmation message containing the bullet's details
    
};

ServerBulletRecevier.prototype.onBulletCollided = function(){
    
};